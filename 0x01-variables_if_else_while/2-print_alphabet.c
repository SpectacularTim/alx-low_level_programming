#include <stdio.h>
/**
 * main - prints lowercase alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet = 'a';

	while (char alphabet <= 'z')
	{
		putchar(alphabet);
		alphabet++;
	}
	putchar("\n");
	Return(0);
}
